#!/usr/bin/env bash
# There is no scripted solution for the bootstrap task: it exists precisely so
# that YOU pick the subscription that gets pinned — a script guessing here is
# how work tenants get touched. Follow prompt.md step by step.
echo "Follow prompt.md — this task is done by hand, on purpose." >&2
exit 1
