# azure-pipelines.yml — a multi-stage Build -> Deploy pipeline. See the prompt.
# Docs: https://learn.microsoft.com/azure/devops/pipelines/process/stages
#
# TODO: add a trigger on main, a pool.vmImage, and a stages: list with a
# Build stage and a Deploy stage (Deploy dependsOn Build), each with a job
# that has steps.
trigger: none

pool:
  vmImage: ubuntu-latest
