# Automate release notes from Git history.
#
# TODO: this workflow currently runs on every push to main and does nothing
# useful. Rework it so that when a release tag is cut, a job generates the
# changelog from Git history and publishes it on the GitHub Release.
name: release-notes

on:
  push:
    branches:
      - main

jobs:
  changelog:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4
      - name: Say hello
        run: echo "TODO generate notes"
